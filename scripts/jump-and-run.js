function draw() {
    var canvas = document.getElementById('jump-and-run');
    var context = canvas.getContext('2d');

    context.canvas.height = window.innerHeight;
    context.canvas.width = window.innerWidth;

}

